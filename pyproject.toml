[build-system]
requires = [
    "setuptools>=68.0",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.setuptools]
# This section disables PEP 621 auto-discovery of dynamic fields
metadata.dynamic = []
